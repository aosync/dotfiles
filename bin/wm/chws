#!/bin/sh

read -r cws < "$WMDIR/cws"

t="$1"

echo "Current" $cws
echo "Target" $t

case $t in
	$cws) ;;
	*)
		ls -1 "$WMDIR/$cws" | xargs mapw -u 2>/dev/null
		ls -1 "$WMDIR/$t" | xargs mapw -m 2>/dev/null
		echo "$t" > "$WMDIR/cws"
		;;
esac
