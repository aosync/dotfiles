#!/bin/sh

wew | while read -r ev; do

# Check if the event is not a resent
[ "$ev" = "$p" ] && continue
p="$ev"

set -- $ev

case $1 in
	CREATE)
		mgw "$2"
		;;
	ENTER)
		echo "Entered window $2"
		wtf "$2"
		;;
	DESTROY)
		umgw "$2"
		;;
esac

done
